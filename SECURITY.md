# אבטחה בתמורה

תמורה היא אפליקציה מקומית. לקוחות, פרויקטים, משימות ושעות נשמרים ב־IndexedDB תחת `%APPDATA%\BouTime`; אין שרת נתונים או חשבון משתמש באפליקציה. בגרסת הדפדפן המידע נשמר באותו דפדפן, מכשיר וכתובת. גיבוי JSON הוא קובץ לא מוצפן שמכיל את המידע האישי.

## הגנות בגרסה 1.4.1

- חלונות Electron פועלים עם sandbox ו־context isolation וללא Node.js בממשק. פקודות IPC מוגבלות לפעולות מוכרות, לחלון המורשה ולמסמך הראשי המקומי.
- ניווט למסמכים חיצוניים, קבצים מקומיים, חלונות חדשים ו־webview נחסם. CSP חוסם סקריפטים מוטמעים, eval, מסגרות ותוכן object. סגנונות inline נדרשים לצבעי הפרויקטים ולתצוגה דינמית; הרשאה זו נוגעת ל־CSS בלבד, לא ל־JavaScript.
- ב־EXE המופץ כבויים מצבי RunAsNode, NODE_OPTIONS ו־Node CLI inspector. טעינת קוד האפליקציה מוגבלת ל־app.asar ונבדקת מול מידע השלמות שמוטמע בקובץ ההפעלה. ראו [Electron Fuses](https://www.electronjs.org/docs/latest/tutorial/fuses) ו־[ASAR Integrity](https://www.electronjs.org/docs/latest/tutorial/asar-integrity).
- עדכונים מגיעים מהמאגר הציבורי הקבוע `huxhkuh/bou-time`. הממשק אינו יכול לבחור כתובת הורדה או קובץ להפעלה. מאומתים שם המתקין, גרסה יציבה, גודל ומבנה SHA-512; קובץ ההתקנה נבדק שוב אחרי אישור המשתמש, לפני הפעלתו. קובץ ששונה או קישור סמלי נדחים. אין הורדה אוטומטית או התקנה בסגירה רגילה.
- גיבויים עוברים בדיקת מבנה, עומק, כמות נתונים, מזהים, קשרים ותאריכים לפני מיזוג. מפתחות מסוכנים כגון `__proto__` נדחים. בדיקות קשרים ומיזוג משתמשות ב־Set כדי למנוע סריקות חוזרות כבדות. ייצוא CSV מנטרל נוסחאות גם אחרי רווחים ותווי בקרה.

## גבולות ההגנה

הקבצים אינם חתומים בתעודת מפרסם ל־Windows. SHA-512 ו־SHA-256 הם בדיקות שלמות, לא חתימה עצמאית של המפרסם: תוקף ששולט במאגר GitHub יכול להחליף גם קובץ וגם מניפסט. חתימת קוד רשמית דורשת תעודת חתימה או שירות חתימה והגדרת מפתח מחוץ למאגר; לא הוגדרו מפתחות או שירותים בתשלום במסגרת ההקשחה.

הנתונים אינם מוצפנים בפני תוכנות שרצות באותו חשבון Windows או בפני מנהל מערכת. אימות ASAR אינו מונע ממי שיכול לשכתב את ה־EXE עצמו להסיר את ההגנות. בדיקה חוזרת של המתקין אינה תחליף להרשאות קבצים ולהגנה על מערכת ההפעלה, ואינה מבטיחה חסינות מפני שינוי מתוזמן אחרי הבדיקה.

חשוב להגן על חשבון GitHub באמצעות אימות דו־שלבי ולהגביל הרשאות כתיבה. הגדרות החשבון, הרשאות המאגר וכללי הגנת הענף לא שונו. מומלץ לשמור גיבויים במקום פרטי, להשתמש בחשבונות Windows נפרדים ולנעול את המחשב כשאינו בשימוש.

## בדיקות ודיווח

`npm test` כולל בדיקות לקלט זדוני, נוסחאות CSV, מטא־נתונים של עדכון ושינוי קובץ אחרי הורדה. `tests/desktop-security.mjs` בודק חסימות ניווט, CSP ו־IPC ותצוגת HTML מיובא כטקסט. `tests/asar-integrity.mjs` משנה עותק בדיקה ודורש שהאפליקציה תסרב להפעילו. `tests/windows-hardened.py` בודק את ה־EXE המקורי דרך ממשק Windows ללא דיבאגר.

לבדיקות Playwright בלבד, `scripts/security-test-copy.mjs` יוצר עותק נפרד ומאפשר בו את Node inspector. ההגנה נשארת כבויה בפני חיבור כזה בגרסה המופצת; אין לשחרר את עותק הבדיקה. בדיקת העדכון המלאה משתמשת גם היא בזהות QA נפרדת עם inspector לצורך הבדיקה.

בבדיקה הנוכחית `npm audit` לא דיווח על פגיעויות ידועות. תוצאה זו אינה הבטחה להיעדר פגיעויות. בדיווח על תקלה יש לציין גרסה ושלבי שחזור ולהשתמש בנתוני דוגמה; אין לצרף גיבויים אמיתיים או סודות ל־Issues ציבוריים.
